cmake_minimum_required(VERSION 2.8)
project( BrilloContraste )
find_package( OpenCV REQUIRED )
add_executable( BrilloContraste BrilloContraste.cpp )
target_link_libraries( BrilloContraste ${OpenCV_LIBS} )
